{
  "name": "flickleaf",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "A browser reader where your hand controls the pace.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.js",
    "dev": "node build.mjs --serve",
    "test:browser": "node tests/browser.mjs",
    "lint:extension": "web-ext lint --source-dir dist",
    "package": "npm run build && web-ext build --source-dir dist --artifacts-dir artifacts --overwrite-dest",
    "build:web": "node build-web.mjs",
    "test:web": "node tests/web.mjs",
    "build:chrome": "node build.mjs --chrome",
    "build:all": "npm run build && npm run build:chrome && npm run build:web && npm run build:safari",
    "package:chrome": "npm run build:chrome && web-ext build --source-dir dist-chrome --artifacts-dir artifacts --filename flickleaf-chrome-{version}.zip --overwrite-dest",
    "test:chrome": "node tests/chrome.mjs",
    "build:safari": "node build.mjs --safari",
    "package:safari": "npm run build:safari && node package-safari.mjs",
    "test:safari": "node tests/safari.mjs"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "pdfjs-dist": "6.3.289"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.1.1",
    "playwright": "^1.63.0",
    "web-ext": "^10.7.0"
  },
  "license": "MIT"
}
